{
  "name": "cloudflare-dropbox-worker",
  "version": "1.0.0",
  "description": "Cloudflare Worker to parse Excel files from Dropbox",
  "main": "src/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "dependencies": {
    "xlsx": "^0.18.5"
  },
  "devDependencies": {
    "wrangler": "^4.51.0"
  }
}
